[project]
name = "greywind"
version = "0.1.0"
description = "灰风 — 桌面 AI 伴侣"
requires-python = ">=3.10"
dependencies = [
    "loguru",
    "openai",
    "anthropic",
    "chardet",
    "pydantic",
    "pyyaml",
    "fastapi",
    "uvicorn[standard]",
    "numpy",
    "edge-tts",
    "requests",
    "jinja2",
    "onnxruntime>=1.19.0",
    "pydub>=0.25.1",
    "Pillow>=10.0.0",
]

[project.optional-dependencies]
browser = ["playwright>=1.58.0"]
desktop = ["pyautogui>=0.9.54", "pyperclip>=1.8.0", "pygetwindow>=0.0.9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/greywind"]
