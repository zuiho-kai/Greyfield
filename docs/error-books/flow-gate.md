# 错题本 — 门控/流程执行

> 适用场景：走门控链、等用户确认、环境准备

### 记录规则

- **条目结构**：❌ 错误做法 + ✅ 正确做法，整条控制在 **5 行以内**
- **❌/✅ 写法**：只写核心规则，禁止多层加粗嵌套（加粗最多一层）
- **`>` 注释**：只放归因和 postmortem 链接，禁止复述场景
- **复犯**：只追加频率标记（🟡×N / 🔴×N）
- **归属**：条目只写本范围内容，跨范围写到对应文件

### DEV-4 跳过流程门控直接编码 `🔴高频×18`

❌ 跳过门控链任一环节直接编码；CR 甩给子 agent 后不自己逐文件复核
✅ 完整门控链缺一不可。CR 必须用独立 agent，主 agent 逐文件过 checklist。改文件前确认工作环境已就绪。每步报告后等用户确认再进下一步

### DEV-5 实施不遵循设计文档 `🟢`

❌ 设计文档定义了接口格式，实施时自己改了字段名
✅ 严格按设计文档实施，需要改设计先更新文档再改代码

### DEV-42 对话开头环境指令未执行就动手 `🟢`

❌ 用户开头说"用 worktree/切分支"，后续遗忘直接在 main 上改
✅ 第一次 Edit/Write/Bash(git) 前回溯对话开头确认工作环境，指定了就先执行，未就绪不动手

### DEV-53 问了用户但不等回答就自己执行 `🟢`

❌ 向用户提问后不等回答就启动操作
✅ 提问 = 交出控制权。提问后停下等回答，用户回答前唯一允许的动作是"等你"

### DEV-61 交付前不做开发自验证 `🟢`

❌ 代码写完直接交给用户测试，用户发现基础问题（模型不显示、编码乱码）
✅ 涉及前端/GUI 的改动，交付前必须自验证：启动后端→启动前端→核心链路跑通→确认无报错。CLI 无法验证的部分明确标注"需手动验证"及验证步骤
