# 错题本 — 通用/流程（索引页，每次必读）

> 按范围拆分为子文件。每次必读本索引，再根据当前任务读对应子文件。

### 记录规则（全局，子文件共享）

- **条目结构**：❌ 错误做法 + ✅ 正确做法，整条控制在 **5 行以内**
- **❌/✅ 写法**：只写核心规则，一句话能说清的不拆两句。禁止多层加粗嵌套（加粗最多一层）
- **`>` 注释**：只放归因和 postmortem 链接，禁止复述犯错场景
- **复犯**：只追加频率标记（🟡×N / 🔴×N），不堆叠每次场景描述
- **归属**：条目只写本范围内容，跨范围写到对应文件，不混入

### 子文件与加载策略

| 文件 | 范围 | 何时读 | 条目 |
|------|------|--------|------|
| `flow-gate.md` | 门控/流程执行、用户交互 | 走门控链、等用户确认、准备环境 | DEV-4,5,42,53 |
| `flow-code-habit.md` | 代码修改习惯 | 改代码、改文档、修 P0/P1、批量操作 | DEV-6,24,29,47 |
| `flow-design.md` | 设计阶段 | 写设计文档、功能设计、架构风险分析 | DEV-38,40,44 |
